@(tagNum: Int)

package asuna
import scala.language.higherKinds

trait Application@{tagNum}[F[_ @for(i <- 2 to tagNum) { , _ }], Tag @for(i <- 1 to tagNum) { , X@{i} }] extends Any {

    def application(context: Context@{tagNum}[F]): F[X1 @for(i <- 2 to tagNum) { , X@{i} }]

}