@(count: Int)

class CaseClassRepMapper@{count}[@for(i <- 1 to count) {
    Rep@{i},
    Data@{i}
    @if(i < count) { , }
}](
    @for(i <- 1 to count) {
        val rep@{i}: Rep@{i},
        val property@{i}: PropertyType[Data@{i}]
        @if(i < count) { , }
    }
) {
}