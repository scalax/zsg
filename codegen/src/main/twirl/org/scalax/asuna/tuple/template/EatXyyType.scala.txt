@(maxItem: Int)

package org.scalax.asuna.mapper

class XyyTupleType0 extends TypeParameter {
  override type H = Item0
  override type T = XyyTupleType0
}

@for(i <- 1 to maxItem) {
    class XyyTupleType@{i}[@for(ii <- 1 to i) { E@{ii} <: TypeParameter @if(ii < i) { , } }] extends TypeParameter {
        override type H = Tuple@{i}[@for(ii <- 1 to i) { E@{ii}#H @if(ii < i) { , } }]
        override type T = XyyTupleType@{i}[@for(ii <- 1 to i) { E@{ii}#T @if(ii < i) { , } }]
    }
}