@(count: Int)

trait Item@{count} [@for(i <- 1 to count) { I@{i} @if(i < count) { , } }] extends Any {
    self =>

    @for(i <- 1 to count) {
        def i@{i}: I@{i}
    }

    def eat[T](xyy: T): Item@{count + 1}[@for(i <- 1 to count) { I@{i} @if(i < count) { , } }, T]

}