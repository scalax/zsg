@(maxItem: Int)

package org.scalax.asuna.mapper

class TypeParameter1[T1] extends TypeParameter {
  override type H = T1
  override type T = EndParameter
}

@for(tagNum <- 2 to maxItem) {
    class TypeParameter@{tagNum}[T1 @for(i <- 2 to tagNum) { , T@{i} }] extends TypeParameter {
      override type H = T1
      override type T = TypeParameter@{tagNum - 1}[@for(i <- 2 to tagNum) { T@{i} @if(i < tagNum) { , } }]
    }
}