@(currentItem: Int)(maxItem: Int)

@if(currentItem == 1) {
context.append[TupleTypeHList0, T1, HListTypeHList1[T1]]
(context.start, t1.application(context), HListTypeHListPlus1.plus1)
}

@if(currentItem > 1 && currentItem < maxItem) {
context.append[HListTypeHList@{currentItem - 1}
[@for(i <- 1 to currentItem - 1) {T@{i} @if(i < (currentItem - 1)) { , }}],
T@{currentItem},
HListTypeHList@{currentItem}
[@for(i <- 1 to currentItem) {T@{i} @if(i < currentItem) { , }}]
](@asuna.codegen.tuple.template.utils.txt.ContextAppend(currentItem - 1)(maxItem),
t@{currentItem}.application(context), HListTypeHListPlus@{currentItem}.plus@{currentItem})
}

@if(currentItem > 1 && currentItem == maxItem) {
context.append[HListTypeHList@{currentItem - 1}
[@for(i <- 1 to currentItem - 1) {T@{i} @if(i < (currentItem - 1)) { , }}],
T@{currentItem},
TupleTypeHList@{currentItem}
[@for(i <- 1 to currentItem) {T@{i} @if(i < currentItem) { , }}]
](@asuna.codegen.tuple.template.utils.txt.ContextAppend(currentItem - 1)(maxItem),
t@{currentItem}.application(context), HListToTupleTypeHListPlus@{currentItem}.plus@{currentItem})
}